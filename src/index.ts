import initialAnimaion from "./utils/initial";
import themeSwitcher from "@/utils/theme-switcher";
import langSwitcher from "@/utils/lang-switcher";


// import triggerScroll from "./utils/lang-charset";

// const langScrollItems = document.querySelectorAll("[data-spec='lang-scroll']");

// console.log(langScrollItems);

// setTimeout(() => {
//   triggerScroll(langScrollItems, "English");
// }, 4000);

initialAnimaion();
themeSwitcher();
langSwitcher();
